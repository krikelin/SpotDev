/****************************
Generated by SpotDev.
Copyright (C) ${Year} ${App.Author}
********************************/
var sp = getSpotifyApi(1);
var models = sp.require("sp://import/scripts/api/models");
var views = sp.require("sp://import/scripts/api/viweas");
var ui = sp.require("sp://import/scripts/api/ui");
var pager = sp.require("sp://import/scripts/api/ui");

exports.init = init;

/*********
Init function
************/
function init() {
	/*
	Write your actions here
	*/
	
	// Inititalize events
	models.application.observe(models.EVENT.ACTIVATE, App.onActivate);
	models.application.observe(models.EVENT.DEACTIVATE, App.onDeActivate);
	models.application.observe(models.EVENT.ARGUMENTSCHANGED, App.onArgumentsChanged);
	models.application.observe(models.EVENT.LINKSCHANGED, App.onALinksChanged);
	models.player.observe(models.EVENT.CHANGE, App.onTrackChange);
	
	// Fire the initialization
	App.onInit();
}

var App = {
	
	/**
	App is deactivated
	*/	
	onDeActivate: function() {
		// Write your deactivation code here
	},
	/**
	App activates
	**/
	onActivate: function() {
		// Write your activation code here
	},
	
	/**
	Occurs when arguments is changed
	This happens either when a user clicks on a spotify link that points to spotify:app:${App.BundleIdentifier}:<section> or
	when an user clicks on a tab.
	**/
	onArgumentsChanged: function() {
		var arguments = models.application.arguments;
		
		
		
		
	},
	/**
	When a link is dropped to the app
	**/
	onLinksChanged: function() {
		// Occurs when links is changed
		var links = models.application.links;
	},
	
	/**
	Occurs when the app is initialized
	*/
	onInit: function() {
	},
	
	/*
	Player events
	*/
	onTrackChange: function(event) {
	}
	
	
};


